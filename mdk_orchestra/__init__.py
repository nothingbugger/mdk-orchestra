"""MDK Orchestra — predictive maintenance for Bitcoin mining via multi-agent AI.

Top-level CLI lives in :mod:`mdk_orchestra.cli`. The heavy lifting — simulator,
deterministic detection, Orchestra agents, dashboard — lives in sibling
top-level packages (``simulator``, ``deterministic_tools``, ``agents``, etc.)
to keep the public-repo layout flat and transparent.
"""

__version__ = "0.1.0"
